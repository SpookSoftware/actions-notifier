{
  "scripts": {
    "test": "bun test",
    "build": "bun build extension/addNotificationButton.js --outdir ./extension/dist --target browser --sourcemap",
    "build:production": "bun build extension/addNotificationButton.js --outdir ./extension/dist --target browser --minify --drop=console",
    "dev": "bun ./watcher.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.0.243",
    "@types/node": "^22.9.0",
    "linkedom": "^0.18.5",
    "p-memoize": "^7.1.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
